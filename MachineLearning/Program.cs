using Microsoft.ML;
using static Microsoft.ML.DataOperationsCatalog;

namespace MachineLearning
{
    internal class Program
    {
        private static void Main(string[] args)
        {

            #region Demo

            //var mlContext = new MLContext();

            //string dataPath = "file.csv";

            //IDataView dataset = mlContext.Data.LoadFromTextFile(dataPath);

            //TrainTestData trainTestSplit = mlContext.Data.TrainTestSplit(dataset);

            //IDataView trainingSet = trainTestSplit.TestSet;

            #endregion
        }
    }
}